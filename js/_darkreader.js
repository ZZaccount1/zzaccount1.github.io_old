// ToDo: It's not working with browserify, try to find an solution. 
// It works only for one frame for whatever reason(i guess)
// Maybe update the whole website to be in dark mode, without using dark reader, just maybe make it manually
const DarkReader = require("darkreader")
DarkReader.enable();

console.log("heya");
